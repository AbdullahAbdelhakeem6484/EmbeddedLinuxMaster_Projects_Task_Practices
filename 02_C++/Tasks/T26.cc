/*******************************************************
* Author: Abdullah Abdelhakeem
* Date: October 1, 2023
* Description: Test all functions of STL
*
* Type: Task
* Language: C++
* Dependencies: None
* IDE: VS Code
* Compiler: G++
* OS: Windows 10
*
* Inputs:
* Outputs:
*
* Algorithm:
*   1.
*
* Note: https://hackingcpp.com/cpp/cheat_sheets.html#hfold0a
*******************************************************/

/*
STL Containers:
std::vector:

push_back()
pop_back()
size()
operator[]
at()
front()
back()
clear()
insert()
erase()
std::list:

push_back()
push_front()
pop_back()
pop_front()
size()
front()
back()
insert()
erase()
std::set:

insert()
find()
erase()
size()
count()
clear()
std::map:

insert()
find()
erase()
operator[]
size()
clear()
std::queue:

push()
pop()
front()
back()
empty()
size()
std::stack:

push()
pop()
top()
empty()
size()
STL Algorithms:
std::for_each:

Apply a function to each element in a range.
std::sort:

Sort elements in a range.
std::find:

Find an element in a range.
std::copy:

Copy elements from one range to another.
std::transform:

Apply an operation to each element in a range and store the result in another range.
std::reverse:

Reverse the order of elements in a range.
std::max_element:

Find the largest element in a range.
std::min_element:

Find the smallest element in a range.
std::accumulate:

Calculate the sum of elements in a range.
std::count:

Count occurrences of a value in a range.
STL Functional:
std::function:

Wrapper for any callable target (function, lambda, bind expression, functor, etc.).
std::bind:

Create a callable object with bound arguments.
std::plus:

Function object for addition.
std::minus:

Function object for subtraction.
std::multiplies:

Function object for multiplication.
std::divides:

Function object for division.
std::modulus:

Function object for modulus.
std::greater:

Function object for greater-than comparison.
std::less:

Function object for less-than comparison.
std::equal_to:

Function object for equality comparison.
*/
#include<iostream>
int main(){
    std::cout << "Hello World" << std::endl;
    return 0;
}